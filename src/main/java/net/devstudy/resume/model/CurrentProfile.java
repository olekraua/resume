package net.devstudy.resume.model;

public class CurrentProfile {
    private Long id;
    public CurrentProfile() {}
    public CurrentProfile(Long id) { this.id = id; }
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
}
